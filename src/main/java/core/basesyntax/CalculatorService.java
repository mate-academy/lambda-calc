package core.basesyntax;

public interface CalculatorService {
    double calculateAddition(double firstArgument, double secondArgument);

    double calculateSubtraction(double firstArgument, double secondArgument);

    double calculateDivision(double firstArgument, double secondArgument);

    double calculateMultiplication(double firstArgument, double secondArgument);

    double calculateRaisingToAPower(double firstArgument, double secondArgument);
}
